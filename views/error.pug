//- extends layout.pug

//- block content

//-   nav.grid-container.portfolio-breadcrumb
//-     a(href='/') ← Back
//-   article.grid-container.portfolio-intro.portfolio-project
//-     .grid-x
//-       .cell.small-6.medium-12.large-12

//-         h1 Error Message: #{error.message}
//-         h2 Status code: #{error.status}
//-         pre= error.stack
//-   nav.grid-container.portfolio-breadcrumb
//-     a(href='/') ← Back
extends layout.pug

block content

    #error
    h1 #{error.message}
    h2 #{error.status}
    pre #{error.stack}